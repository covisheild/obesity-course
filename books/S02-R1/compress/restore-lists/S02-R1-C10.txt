# B-C11-1: "scalar" is used from C11 on; the cut removed its definition here
definition.text::A single number used this way is called a scalar.
# B-C10-1: says a dot product is a weighted sum of entries
definition.text::The dot product is a weighted sum of the entries of one vector
# B-C10-2: vector subtraction shown entry by entry (P2 needs it)
illustration.body::A clinic records four things about each patient
illustration.body::They are age in years, body weight in kg
illustration.body::One patient's row, made up for this example, is a vector.
illustration.body::Take the same patient a year later
illustration.body::Subtract entry by entry.
illustration.body::The change vector y − x is (1, −3, 0, −4).
