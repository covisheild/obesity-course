# S02-R1-C07 · external reader feedback, 24 Sep 2026 (triaged by the conductor)

1. **gap** — Euler / forward step (line ~344): "A smaller step drifts less." Add that a different
   step size gives a different numerical answer, so a model run is also a choice of step, and that
   the next rung teaches when a step makes the answer run away. Use the section's own numbers
   (step 1 against the formula) if they show it.
   Fixer: added four short sentences after "A smaller step drifts less.": a different step gives a different answer (day 90: one-day steps 95.00, thirty-day steps 94.83, formula 95.01, all recomputed in Python), a model run is also a choice of step, and the next rung shows when too large a step runs away. Build --check blocking 0.
   Verifier: closed. Recomputed in Python: one-day steps 95.0007, thirty-day steps 94.8254, formula 95.0064 at day 90. The sentences read plainly.
