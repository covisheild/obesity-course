# Audit defects · S02-R1-C14 · Expectation

Auditor: Task 3 (batch audit-5), 24 Sep 2026. Field indices are 0-based. Practice problems are
numbered 1 to 14 in the order the reader sees them, with their `level` in brackets.

**Quotes.** All ten `definition.references` quotes were found verbatim (whitespace-normalised):
six in `openintro_stats_4e.txt` (§3.4.1 text and box, footnote 50, §3.4.3 Example 3.61, the
LINEAR COMBINATIONS box, footnote 55), three in `openstax_intro_stats_2e.txt` (§4.2: the Law of
Large Numbers paragraph, the soccer example's 1.1, the "equally likely" sentence) and one in
`hall_2012_ajcn.txt` (Question 1, "ES is the rate of change ..."). Locators match. The one number
quote (1.1) states its figure. Practice 6 (level 4) rests on FAO/WHO/UNU: "a male with a PAL of
1.75 and a mean BMR of 7.10 MJ/day ... 1.75 × 7.10 = 12.42 MJ/day" is verbatim in
`fao_who_unu_2004.txt`, chapter 5. Two quotes do not carry their claim (items 1 and 10). No quote
needed editing; none was edited.

**Arithmetic.** Every line recomputed in Python (`/home/claude/scratch-audit-5/arith.py`). All
correct:
- Illustration: E[X] = −60 + 0 + 80 + 50 = 70; unweighted 400/4 = 100; week 490; Y = 140 − x gives
  440, 140, −60, −360 and E[Y] = 70; shift 190, 1,330; village 50 × 0.1 = 5.0; survey 23/4 = 5.75.
- Practice 1: 2.1. 2: 0.25. 3: 11, −3, 0. 4: 3, 5, 14. 5: 0.25. 6: 12.425. 7: −1,050, −4,200.
  8: 150, 1,050, 210, 840. 9: 6.0. 10: 70. 11: 490. 12: 70, 1.65, 25.71, then 22.22, 27.78, 25.0.
  13: 4.0. 14: 36,500.

**Mathematics.** E[aX + b] = aE[X] + b and E[X + Y] = E[X] + E[Y] with no independence condition are
right as stated, and so is the warning on products and ratios (OpenIntro footnote 55). Neither
rule is derived (item 4). "Only when all the values are equally likely" is wrong as a necessary
condition (items 1, 2).

**Ladder.** Levels 1, 2, 2, 3, 3, 4, 5, 5, 6, 7, 7, 8, 9, 10: all four bands, climbing. Fourteen
is right for the moves (weighted sum, the two rules, reverse from E[X], scaling, n days, estimate
versus parameter). Direction reversed in practice 5.

**Figures.** All four PNGs viewed; every number recomputed.
- `surplus-balance`: 0.2, 0.3, 0.4, 0.1 sum to 1; weighted sum 70; mean(x) 100. See item 12.
- `week-expected`: 70x, 500x, −300x through day 0, 1, 7; 3,500 and −2,100 derived. Right.
- `dependent-days`: four points on x + y = 140; both weighted sums 70. See item 13.
- `logged-shift`: 70x and 190x; 1,330 − 490 = 840 = 7 × 120. Right.

**Currency.** Derivable; nothing dated in the teaching. Practice 6 quotes the FAO/WHO/UNU 2004
worked example: re-check at the next joint FAO/WHO/UNU energy requirements report.

**Build.** `python check/build.py --check`: blocking 0; no C14 warning.

---

1. **error** · `definition.text`, "It is the ordinary mean of `B0-R0-C28` only when all the values
   are equally likely." (from compression hole 7; confirmed, fix corrected).
   - In the village the values are not equally likely (P(X = 5) is 0.3), yet E[X] = 5.0 is the
     ordinary mean of the ten households. The hole's second proposed fix ("the ordinary mean of
     the distinct values only when all the values are equally likely") is still wrong: "only
     when" fails for any symmetric distribution. C15's own reading error, −10, 0 or 10 with
     0.25, 0.5, 0.25, has E[X] = 0, the plain mean of the three values.
   - The cited OpenStax sentence gives only the "when": "When all outcomes in the probability
     distribution are equally likely, these formulas coincide with the mean ...".
   - Fix: "When every listed case is equally likely, as with the households of the village below,
     E[X] is the ordinary mean of `B0-R0-C28`. In general it is not."
   - Recurring: yes (a quote made to stand behind more than it says; SELFCHECK 1).
   - Fixer: replaced with the audit's two sentences ("When every listed case is equally likely, as with the households of the village below, E[X] is the ordinary mean of `B0-R0-C28`. In general it is not."); OpenStax reference note updated to say it carries only the "when".
   Verifier: closed

2. **error** · `must_know[1].point` (hole 7 called it correct; it is not, for the reason in item 1).
   - Claim: "Averaging the possible values without their probabilities gives the expected value
     only when every value is equally likely."
   - Fix: "Averaging the possible values without their probabilities can be trusted only when
     every value is equally likely. Weight each value by its probability, every time."
   - Fixer: `must_know[1]` now reads "... can be trusted only when every value is equally likely. Weight each value by its probability, every time."
   Verifier: closed

3. **error** · practice 14 (level 10), prompt and answer.
   - Prompt: "A surplus of just 100 kcal a day adds up to 36,500 kcal in a year, and all of it is
     stored." The answer grants "The arithmetic of the expected value is right on that condition",
     then ends "What is wrong is the 36,500."
   - If the surplus really is 100 every day, 36,500 is right. The answer's argument (expenditure
     rises, "the same intake gives a shrinking surplus") needs the claim to be about extra intake,
     which the prompt does not say. Compression gap B-C14-5 flagged the route as unclear; the
     restored `analogy_breaks_when` sentences do not repair the prompt.
   - Fix: prompt → "Eating just 100 kcal a day more adds up to 36,500 kcal in a year, and all of
     it is stored." Answer, first paragraph → "What to compute: the talk treats the extra 100 kcal
     of intake as a surplus of 100 on every day. Then the expected surplus over 365 days is 365
     times 100." The rest stands.
   - Recurring: yes (the record does what it says cannot be done; SELFCHECK 5).
   - Fixer: prompt and first answer paragraph replaced with the audit's wording; rest of the answer unchanged (365 × 100 = 36,500 recomputed).
   Verifier: closed

4. **gap** · `definition.text`, "Two rules follow from the definition." (residue of compression gap
   B-C14-1, not carried to DEFECTS.md).
   - The concept is `derivable`, and neither rule is derived. The general proof of the sum rule
     was cut and its restore failed validation (RESTORE-DECISIONS-b). The dependent-days case
     checks the sum rule once. E[aX + b] is only stated, yet C15's derivation leans on it: "First,
     the expected value of aX + b is a times E[X] plus b. The last section showed that." That
     pointer is false.
   - Fix: after "E[aX + b] = aE[X] + b", add a working block
     `Σ (ax + b) P(X = x) = a Σ x P(X = x) + b Σ P(X = x) = aE[X] + b`, and "The last step uses
     the fact that the probabilities add to 1." After the sum rule, add "The two dependent days
     below check it; this book does not prove it in general." Or restore the original's proof
     in shorter sentences.
   - Recurring: yes (a derivation that skips, SELFCHECK 9; a false pointer, S01-R1-C05 items 1
     and 9).
   - Fixer: both rules now shown. First rule: one working line Σ (ax + b) P(X = x) = a Σ x P(X = x) + b Σ P(X = x) = aE[X] + b, plus "The last step uses the fact that the probabilities add to 1." Sum rule: the original's outcome-by-outcome proof restored in five short sentences, ending "No step asked whether X and Y are independent." C15's "The last section showed that" is now true.
   Verifier: closed

5. **gap** · `definition.text` and later: "independent" for random variables.
   - C14 uses "independent" of random variables throughout (definition, illustration,
     `must_know[2]`, practice 4, 7 and 14). C15 is where it is defined ("Two
     random variables are independent when every event about one is independent ... of every
     event about the other"). Book 0 D2 (`B0-R0-C25`) defines it only for events.
   - Fix: at first use, "whether or not X and Y are independent (knowing the value of one tells
     you nothing about the other, the idea of `B0-R0-C25` carried to random variables)". C15's
     sentence can then say "As section 14 said, ...".
   - Recurring: yes (a term used before it is explained; SELFCHECK 14).
   - Fixer: at first use, "Independent means that knowing one tells you nothing about the other, the idea of `B0-R0-C25` carried to random variables." C15 not edited (another fixer's record).
   Verifier: closed

6. **error** · `exercises[0].answer`, "EI is energy intake, and it has an energy unit, kcal/day."
   - kcal/day is energy per unit of time, not an energy unit. This book (C01, C05) makes the
     difference the point of reading Hall's equation.
   - Fix: "... and it is a rate, in kcal/day."
   - Recurring: yes (a wrong unit; S01-R1-C05 item 8).
   - Fixer: "EI is energy intake, and it is a rate, in kcal/day."
   Verifier: closed

7. **style** · `definition.text`, "E with a label, as in EI, EE or ES, is energy." Notation drift.
   - In this book ES is the store (C05: "ES(b) minus ES(a)") and in Hall's equation it is a rate
     (C01, C05, and the very quote cited here: "ES is the rate of change in the body's
     macronutrient stores"). "Is energy" blurs the distinction the book has drawn.
   - Fix: "... is an energy quantity: a store in kcal, or a rate in kcal a day."
   - Fixer: "... is an energy quantity: a store in kcal, or a rate in kcal a day."
   Verifier: closed

8. **gap** · `concept_deps: [S01-R1-C05, S02-R1-C01, S02-R1-C13]`.
   - Practice 14's answer points to `S01-R1-C04` and `S01-R1-C07`. Neither is in `concept_deps`.
   - Fix: add both.
   - Recurring: yes (SELFCHECK 8; S01-R1-C05 item 10).
   - Fixer: concept_deps now [S01-R1-C04, S01-R1-C05, S01-R1-C07, S02-R1-C01, S02-R1-C13].
   Verifier: closed

9. **gap** · practice 9 (level 6) answer, "picking with replacement" and "the law of large
   numbers".
   - "With replacement" is explained nowhere a reader of Book 0, Book 1 or this book has met it
     (it appears in Book 0 only in a reference note). The law of large numbers is named only here;
     the definition's "It is the long-run average" is where it belongs.
   - Fix: "picking with replacement (a household can be picked more than once)"; in the
     definition, "It is the long-run average: over many repeats, the average of the values
     settles towards E[X], the law of large numbers."
   - Recurring: yes (SELFCHECK 14).
   - Fixer: practice 9 gloss "(a household can be picked more than once)" added; definition now "It is the long-run average: repeat the chance process many times, and the average of the values X takes settles towards E[X]. That is the law of large numbers." (next sentence's "It" changed to "E[X]").
   Verifier: closed

10. **gap** · `definition.references[8]` (OpenIntro footnote 50, "xf(x)dx ...").
    - Its note says "The integral form, named in one sentence and not worked." That sentence was
      cut; nothing in the record now speaks of a continuous expected value. The reference
      vouches for no claim.
    - Fix: delete the reference (compression ruled B-C14-3 not a defect, so do not restore the
      sentence).
    - Recurring: yes (a quote that does not carry a claim in the text; SELFCHECK 1).
    - Fixer: footnote-50 reference deleted.
    Verifier: closed

11. **style** · `definition.text`, "x1, x2, ..., xk" and "P(X = x1)".
    - C01 writes subscripts with an underscore (BW_0, x_i), and this record's own exercise writes
      x_i. "x1" is the side-by-side form C01 says means multiplication.
    - Fix: x_1, x_2, ..., x_k throughout the paragraph.
    - Fixer: withdrawn: superseded by conductor decision 4 (24 Sep 2026): "No underscores in prose. A label is written straight after the letter"; x1, x2, ..., xk already follow it. The exercise's quoted methods line keeps the paper's x_i, since the exercise is about reading that notation.
    Verifier: closed

12. **style** · figure `s02-r1-c14-surplus-balance.png`, caption "where the four bars would
    balance" (compression gap B-C14-4, ruled a figure matter).
    - The prose never says that E[X] is a balance point.
    - Fix: add to the illustration after "E[X] is 70 kcal a day.": "It is where the four bars
      would balance, each weighted by its probability." (OpenIntro §3.4.1 calls the mean "the
      balancing point", if a quote is wanted.)
    - Fixer: added after "E[X] is 70 kcal a day.": "It is where the four bars would balance, each weighted by its probability."
    Verifier: closed

13. **style** · figure `s02-r1-c14-dependent-days.png` legend "y (kcal/day)" (from conductor figure
    note 4; confirmed).
    - The legend is the table's column header, and "y" alone does not tell the reader it is day
      2's surplus.
    - Fix: rename the table's second column "day 2, y (kcal/day)" and redraw, or give the
      series a name in the spec if the tool allows it.
    - Fixer: table columns renamed "day 1, x (kcal/day)" and "day 2, y (kcal/day)"; redrawn and viewed, legend reads "day 2, y (kcal/day)". Also (not a listed defect) the first illustration table rendered as a working block because its header holds "="; rewritten with pipes so it renders as a table.
    Verifier: closed
