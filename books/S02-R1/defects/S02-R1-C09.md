# Audit defects · S02-R1-C09 · Highest and lowest points: slope zero and curvature

Auditor: Task 3 (audit-3), 24 Sep 2026. Field indices are 0-based. Practice problems are numbered
P1 to P16 in the order the reader sees them, with their `level` in brackets. "Recurring" means the
kind is on `check/SELFCHECK.md` (item number given).

**Quotes.** All 8 definition quotes are verbatim in their mapped files
(`openstax_calculus_v1_s02.txt`, `openstax_intro_stats_2e.txt`). The record states no number about
the world (`numbers: []`; every data set is said to be made up). **One quote edited:** reference 0
now reads "is undefined. In other words, local extrema can only occur at critical points." (same
sentence pair, verbatim), so it carries the "or does not exist" half of the definition as well as
the candidates claim. Reference 2 carries nothing the record says (item 3). No held passage states
that the mean minimises Σ (x − m)², as the record's note says; it is derived on the page and the
derivation is right (items 6 and 9 concern how it is shown, not whether it holds).

**Textbook check** against OpenStax Calculus Vol. 1 §4.3 and §4.5 as held. Critical point: the
book says "Let c be an interior point in the domain of f ... f′(c)=0 or f′(c) is undefined"; the
record says "inside the range" (item 1). First derivative test: the three cases match. Second
derivative test: the three cases match, including "inconclusive" at f″(c) = 0; the book's condition
that f″ be continuous near c is dropped (item 13). Location of absolute extrema at endpoints or
critical points: matches the illustration and P5, P12. x³ at 0 and x⁴ at 0 match the book's own
examples.

**Arithmetic.** Every line recomputed in Python (`/home/claude/scratch-audit-3/c09.py`): the cubic
table (−2, 2, 0, −2, 2, 18); S(m) at 70.2 to 71.0 (1.20, 0.60, 0.40, 0.60, 1.20); mean 70.6;
variance 0.1, SD 0.316; 5 × 0.16 and 20 × 0.16; the quadratic expansion f(c + h) = f(c) + (2pc +
q)h + ph². P1–P16 every line (P6 mean 7, sum 46; P7 88.7, 0.18, 0.45 both routes; P8 20,000; P9 1
and 4; P10 S(70) = 1, consistent with Σx² = 1 + 4 × 70² = 19,601; P12 −5, 0, −1, 4; P16 mean 70.8,
median 70.4, 4.10 against 4.90, 3.6 against 2.6, and the mean above four of five readings). All
correct. One question asked is not answered (item 4).

**Figures.** All four specs recomputed: `cubic-critical-points` fit x³ − 3x through the six table
points; `least-squares-valley` 0.40 + 5(m − 70.6)² through all five; `level-not-turning` x³ and 3x²
at −2..2 (−8, −1, 0, 1, 8; 12, 3, 0, 3, 12); `valley-sharpness` 5h² and 20h² at ±0.2, ±0.4 (0.20,
0.80; 0.8, 3.2). Captions match the text. Looked at each PNG; one missing label (item 4).

**Ladder.** 16 problems, levels 1,2,2,3,3,3,4,5,6,6,7,7,8,8,9,10: all four bands, climbing,
direction reversed (P10 from the formula back to n and the mean). Size fits the moves (find, sort,
compare ends, least squares, curvature).

**Currency.** Nothing dated, institutional or empirical. None.

1. **error** (from compression hole A-C09-2) · "range" for the inputs: `definition.text`, "an input
   c, inside the range"; `illustration.body`, "both ends of the range", "over this range", "an end
   of the range"; `must_know[1].point`, "Over a range of inputs"; P5 [3] "over that range"; P8 [5]
   answer "The range of x runs from 0 to 200"; P15 [9] answer "the tested range", "the top of the
   range". **What Book 0 and the textbook say:** Book 0 section B0-R0-C18 defines the range as the
   outputs and the domain as the inputs, and warns of "the trap in the second word"; OpenStax says
   "an interior point in the domain of f". **Fix:** definition, "an input c inside the domain, not
   at one of its ends"; elsewhere "interval" or "stretch of inputs" (P15 "the tested doses"); add
   B0-R0-C18 to `ground_floor_deps`. **Recurring** (SELFCHECK 7).

    Fixer: definition now "an input c inside the domain, not at one of its ends"; "range" replaced by "interval" in the illustration, must_know[1], P5, P12, retrieval item 1 and the figure caption; P8 "x can run from 0 to 200"; P15 and must_know[1] "the tested doses"; B0-R0-C18 added to ground_floor_deps.

2. **gap** · `definition.text`, the critical-point sentence. No reference's quote states the
   definition, because the held line is flattened ("Let cc be an interior point in the domain of
   f.f. We say that cc is a critical number of ff if f′(c)=0..."). **Fix:** add a reference,
   locator "§4.3, Local Extrema and Critical Points, Definition", quote `be an interior point in the
   domain of`, with a note that the formulas are flattened. It also documents item 1's "domain".

    Fixer: added reference (§4.3, Definition), quote "be an interior point in the domain of", verified in the held file, with a note on the flattened formulas.

3. **gap** · `definition.references[2]`, quote "First, the term *absolute* here does not refer to
   absolute value." The record never uses "absolute", so the quote vouches for nothing it says.
   **Fix:** retarget it to §4.3, "Locating Absolute Extrema over a Closed Interval", step 3, quote
   `Therefore, the largest of these values is the absolute maximum of`, which carries the
   illustration's and `must_know[1]`'s compare-the-ends-and-the-critical-points move. **Recurring**
   (SELFCHECK 1).

    Fixer: reference retargeted to §4.3 "Locating Absolute Extrema over a Closed Interval", step 3, quote "Therefore, the largest of these values is the absolute maximum of" (verbatim in held file).

4. **gap** · `illustration.body`, "suppose you want its highest and lowest values for x anywhere from
   −2 to 3", then only the highest is given. **Arithmetic:** the lowest is −2, reached both at the
   local minimum x = 1 and at the end x = −2 (table). The figure labels only "local minimum, −2".
   **Fix:** after "The overall maximum over this range is 18, at x = 3.", add "The overall minimum is
   −2. It is reached twice: at the local minimum x = 1, and at the end x = −2." Add a label "−2 at
   the end" at (−2, −2) in `s02-r1-c09-cubic-critical-points`. **Recurring** (SELFCHECK 10).

    Fixer: added the overall-minimum sentences after the overall maximum; figure label "−2 at the end" at (−2, −2) added, redrawn and checked.

5. **gap** (from compression hole A-C09-3) · `illustration.body` working, "x² = 1" then "x = 1 or x =
   −1"; the same move in P3 [2] answer, "x² = 4, and x = 2 or x = −2". Book 0 teaches that two
   negatives multiply to a positive (B0-R0-C01) and square roots (B0-R0-C06), but nowhere that a
   square has two roots. **Fix:** one working line, "1 × 1 = 1 and (−1) × (−1) = 1, so both
   solve it." **Recurring** (SELFCHECK 9).

    Fixer: working lines "1 times 1 = 1" and "-1 times -1 = 1" added before "x = 1 or x = −1"; P3 answer now says both 2 times 2 and −2 times −2 are 4.

6. **gap** (from compression hole A-C09-4) · `illustration.body`, "S(m) = Σ (x − m)²", "The
   candidate m is the same number in every term, so it comes outside the sum. And m² is added once
   for each reading, n times in all." Section 1 taught Σ with a counter and limits; this Σ has
   neither, and both moves are asserted. `exercises[0]` uses the same bare Σ. **Fix:** write S(m) =
   Σ[i = 1 to n] (x_i − m)², and show the two moves on the five weighings: "−2m × 70.4 − 2m × 70.6 −
   ... = −2m × (70.4 + 70.6 + ...)", and "m² + m² + m² + m² + m² = 5m²". **Recurring** (SELFCHECK 9).

    Fixer: S(m) = Σ[i = 1 to n] (xi − m)² (no underscore, per decision 4), with one line saying the counter is left off from there on; the two moves shown on the five weighings in a working block; exercise 1's first line uses the full form.

7. **style** (from compression hole A-C09-5) · `illustration.body`, "A rate held at zero marks a
   settling point."; `must_know[5].point`, "A slope of zero on a weight curve is not a settling
   point."; P14 [8] answer, "It is not a settling point." Section 8 uses "settling point" only as the
   name of Hall and Guo's model; its word for the idea is equilibrium. (P14's prompt may keep
   "settling point": it is the wrong argument's own word.) **Fix:** "equilibrium" in the three
   places. **Recurring** (SELFCHECK 7).

    Fixer: "equilibrium" in the illustration, must_know[5] and P14's answer; P14 prompt keeps "settling point".

8. **gap** (from compression hole A-C09-6) · P8 [5], the fencing problem. The answer's first move,
   writing the area as a function of one side from the fixed 400 m, is taught nowhere. **Fix:** one
   sentence in the illustration or definition's neighbourhood, not the prompt: "When a quantity is
   fixed, such as a total length, write one unknown in terms of the other before you
   differentiate." Confirmed.

    Fixer: added to the illustration after the cubic: "Some problems tie two unknowns by a fixed amount, such as a total length of fence. Write one unknown in terms of the other before you differentiate."

9. **style** (from compression hole A-C09-7, reclassified from gap) · P10 [6], "S(m) = 4m² − 560m +
   19,601 ... How many readings were there?" The illustration and exercise 1 both display S(m) = Σ
   x² − 2m Σ x + n m², so the reader can read n and Σ x off by lining the two up; the answer shows
   it and checks by the derivative. What is untaught is only the phrase "compare with the general
   form". **Fix:** in the illustration, after the S(m) working: "Read the formula term by term: the
   number in front of m² is n, and the number in front of m is −2 Σ x."

    Fixer: added the auditor's sentence after the S(m) = Σ x² − 2m Σ x + n m² working.

10. **style** (from compression hole A-C09-8; withdrawn in part) · The hole said the median is never
    named and that "x^2" sits beside "x²". **Withdrawn:** Book 0 section B0-R0-C28, which this
    section lists in `ground_floor_deps`, defines the median; and no caret remains in the record's
    reader-facing text (only in figure specs, which render as superscripts). **Residual:**
    `analogy_breaks_when`, "a different centre can come out best", should name it, so P16's move
    has been set up: "the median, from Book 0, can come out best".

    Fixer: analogy_breaks_when now reads "the median of `B0-R0-C28` can come out best".

11. **gap** · terms used before they are explained. "Local maximum" and "local minimum" appear in
    `definition.text` and are explained only halfway through the illustration ("only a peak among
    its neighbours"). "Second derivative test" is used in the illustration ("the second derivative
    test decides nothing") and P4 [3], but the definition calls it only "this test". **Fix:** in
    `simplified_explanation`, "A top that is higher than the ground near it is a local maximum. The
    highest point over the whole stretch is the overall maximum."; in the definition, "This is the
    second derivative test." **Recurring** (SELFCHECK 14).

    Fixer: simplified_explanation gains the local/overall maximum sentences; definition ends "This is the second derivative test."

12. **style** · `illustration.body`, "The same step of 0.4 kg would then raise S by 20 times 0.16".
    No step of 0.4 kg has been mentioned; the table implies it. **Fix:** before it, "A step of 0.4
    kg, to 70.2 or 71.0, raises S by 5 times 0.16, which is 0.80, as the table shows."

    Fixer: added the 0.4 kg step sentence and a working block (0.4 times 0.4 = 0.16; 5 times 0.16 = 0.80; 0.40 plus 0.80 = 1.20); the 20-readings sentence now says "the same step".

13. **style** · `definition.text`, the second derivative test. OpenStax states it for f″ "continuous
    over an interval containing c"; the record drops the condition. At this rung every example is a
    polynomial, so nothing breaks, but `definition.text` is the field held to technical exactness.
    **Fix:** "Suppose f′(c) = 0, and f″ is continuous near c."

    Fixer: definition now "Suppose f′(c) = 0, and f″ is continuous near c."; added reference quote "is continuous over an interval containing" (§4.5, verbatim in held file).

14. **style** · `must_know[4].point` (consequence), "The size of the second derivative at a minimum
    says how sharply the best value is pinned down. So more readings give a sharper valley, and a
    value off the mean costs more." It names nothing the reader will do, say, accept or refuse.
    **Fix:** either move it to the illustration, or make it an action: "When a paper reports a
    best-fit value, ask how fast the fit gets worse away from it. A flat valley means the data barely
    pin the value down." **Recurring** (SELFCHECK 11).

    Fixer: must_know[4] replaced with the auditor's action wording (best-fit value, flat valley); the sharpness point stays in the illustration.
